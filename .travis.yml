language: python
python:
  - "3.5"
# command to install dependencies
install: "pip install -r excel-app/requirements.txt"
# command to run tests
script: py.test --cov=excel-app/

before_install:
  - pip install codecov
after_success:
  - codecov
